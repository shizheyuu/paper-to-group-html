---
name: paper-to-group-html
description: 将一篇或多篇论文 PDF（可同时包含主论文与 Supporting Information）整理为中文讲解为主、简洁学术白底、完全离线的单文件 HTML 组会幻灯片。用于用户说“把这篇 PDF 做成组会 HTML”“做文献组会 PPT”“将论文转成 HTML 幻灯片”等请求；默认面向化学实验论文，特别适配稀土发光材料、光物理、纳米材料与 NIR 成像，同时能按综述或理论论文的内容结构自适应。
---

# 文献 PDF 转组会 HTML

将论文读透后再设计叙事，不把固定目录机械套在每篇文章上。最终交付一个可搬运、可离线打开的 `.html` 文件。

## 必须遵守

- 默认中文讲解，保留必要英文缩写、材料式、能级符号、图题术语和单位。
- 默认单篇精读；输入多篇时自动切换为横向比较或主论文 + SI 模式。
- 识别主论文与 SI。以主论文建立叙事，只用 SI 补充实验条件、表征细节和数据；若只有 SI，先明确提醒内容不完整。
- 围绕文章内容本身，不强制加入批判性分析、演讲者备注或“一句话结论”。
- 正式生成前必须先在对话中讲清楚文章内容，并展示“文章自身提纲”和“拟采用的 PPT 提纲”；等待用户确认或提出修改后再生成 HTML。
- 页数按论文信息量决定，以约 20 分钟为软目标，通常 10–16 页。
- 默认达到“能够支撑完整组会讲解”的深度：不仅陈列结果，还解释设计原因、读图方法、对照关系和证据链。内容不足时优先增加页面，不把解释压缩成口号式短句。
- 复用论文关键图、光谱、结构式、能级图和表格；在图注中标注 `原文 Figure/Table + PDF 页码`。
- 图片、样式和脚本必须全部内嵌，禁止 CDN、远程字体、外链图片或运行时网络请求。
- 默认把最终 HTML 写入 `~/Desktop/组会/`；若该目录不可写或用户指定其他位置，则使用用户给出的目录。
- 开始前读取 [references/content-guide.md](references/content-guide.md)。

## 工作流

### 1. 盘点输入

1. 列出所有 PDF，检查元数据、页数、标题与正文标题。
2. 将文件标记为 `main`、`si` 或 `related`。
3. 若输入含多篇主论文，判断是同主题比较、发展脉络还是方法对照；不确定时只问一个最影响叙事的问题。

### 2. 完整阅读与证据表

1. 使用 PDF 工具提取全文，同时把所有包含关键 Figure/Table 的页面渲染为图片。
2. 记录：研究问题、设计逻辑、材料体系、合成/构筑、表征、光物理机制、关键定量结果、应用验证、作者结论。
3. 对每张核心图写下“这张图测了什么—应看哪里—支持什么结论—如何连接下一步”，用于决定页面讲述深度。
4. 为每个将进入幻灯片的数字建立来源映射：原文句子或图表、PDF 页码、主文或 SI。
5. 联网补充仅限元数据、必要背景和术语解释。把补充信息与论文原始结论明确区分，禁止用外部信息替代论文证据。

### 3. 选择叙事结构

按论文内容自适应：

- 实验论文：背景/瓶颈 → 设计策略 → 材料与结构证据 → 性能 → 机制 → 应用验证 → 作者结论。
- 稀土发光材料：优先检查敏化剂/激活剂、宿主与晶相、掺杂浓度、核壳/界面、激发与发射波长、寿命、量子产率、功率依赖、能量传递路径、组织穿透或成像指标。
- 综述：领域问题 → 分类框架 → 代表路线 → 横向比较 → 趋势与总结。
- 多论文：共同问题 → 每篇核心策略 → 对齐维度比较 → 综合结论。不要逐篇做互不相干的摘要。

先生成标题级大纲。每页只承载一个中心判断；删掉不能推动主线的实验细枝末节。

不要把页面写成“结论清单”。对核心结果至少提供一层解释，例如比较对象、变量变化、能级匹配、对照样品、动力学信号或成像条件。若一个关键机制需要两页才能讲清，拆成两页。

### 4. 生成前确认门

在创建 HTML、裁切最终图片或写入成品目录之前，向用户依次展示：

1. **这篇文章在说什么**：用 3–6 段中文讲清研究动机、核心策略、关键证据和最终结果，必要术语顺手解释。
2. **文章自身提纲**：按作者的论证顺序列出章节/结果模块，不等同于照抄论文目录。
3. **拟采用的 PPT 提纲**：列出预计页数、每页标题和该页承担的叙事任务，并说明删减或合并了什么。

最后只问用户是否按该提纲生成。用户确认前暂停生成；用户提出修改时先更新提纲再继续。

### 5. 准备图片

1. 优先裁切原文 Figure 的完整可读区域，避免整页截图。
2. 保留坐标轴、图例、比例尺、panel 字母和必要图注信息。
3. 不擅自重绘可能改变科学含义的光谱、能级图或结构式。
4. 可用 `scripts/crop_pdf_figure.py` 从渲染页裁图；裁切后检查分辨率和边界。

### 6. 生成单文件 HTML

1. 按 `scripts/build_deck.py --help` 创建 JSON 清单并构建 HTML。
2. 使用 16:9 画布、白底、深蓝文字、少量青绿色强调；不要用暗色科技模板。
3. 文本字号保证投屏可读；标题与正文按内容密度自适应，正文通常为 22–30 px；一页不超过 5 个主要要点。
4. 使用卡片、流程、指标块和图文双栏来表达关系，避免把正文堆成论文摘要；页面占用适中，既不能大面积无意义留白，也不能贴边或拥挤。
5. 所有论文图片可点击放大；使用全屏遮罩展示高清内嵌图，支持点击空白处、关闭按钮或 `Esc` 退出。
6. 确保方向键/空格翻页、页码、进度条、全屏按钮可用；不添加演讲者视图或备注面板。

### 7. 验证

1. 运行 `scripts/verify_html.py <output.html>`，要求无外部资源、无占位符、无缺失内嵌图片。
2. 在浏览器中检查至少：封面、最密集文本页、每种布局、含光谱/能级图页、最后一页。
3. 分别检查 16:9 桌面视口和较小视口，确认无溢出、裁切、重叠、过小图例或乱码。
4. 实际测试前进、后退、空格、全屏按钮，以及至少一张图片的放大和关闭。
5. 若发现问题，修改并重新验证；不得只凭源码判断视觉质量。

## 交付

交付最终 `.html`，简要说明页数、主线和离线特性。若同时输入主文与 SI，说明哪些内容由 SI 补充。不要交付临时裁图、JSON 清单、渲染页或 QA 截图。
